#!/bin/bash
DECORAION_PATH="$HOME.config/hypr/hyprland.conf"
if grep -q "active_opacity = 1" "$DECORAION_PATH"; then
    sed -i 's/active_opacity = 1/active_opacity = 0.9/' $DECORAION_PATH
else
    sed -i 's/active_opacity = 0.9/active_opacity = 1/' $DECORAION_PATH
fi
